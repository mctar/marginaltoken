"""Phase 2 placeholder for OpenRouter per-host endpoint collection.

The future Spreads view will read
GET /api/v1/models/{author}/{slug}/endpoints. Phase 1 deliberately does not
fetch, normalize, or publish that data.
"""
